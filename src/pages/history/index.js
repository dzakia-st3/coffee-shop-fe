import History from '../../component/history/history'
import Layout from '../../layouts/layouts'

export default function HistoryPage() {
  return (
    <Layout title='History' withNavbar withFooter>
      <History />
    </Layout>
  )
}